This is an [iTerm2](http://www.iterm2.com/) workflow for [Alfred.app](http://alfredapp.com).

This workflow is an action on a folder that allows you to open the path of that folder into a new session in iTerm.

To use, select a folder in Alfred then press → to open the action panel and select "Open in iTerm".